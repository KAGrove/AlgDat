package forelesninger;

public class F1_Quicksort {

/*    void quicksort(int[] a, int fra, int til){
        if(fra +1 >= til){                  // Mulig +1 er unødvendig
            return;

        }
        int p = byttPivotBakerst(a);    // Steg 1. Skal returnere verdien som står bakerst
        int six = partisjoner(a,p);     // Sort Index. Indexen der pivotelementet skal inn. Steg 2
        byttPivotBakerst(a, six);       // Steg 3
        quicksort(a, fra, six);
        quicksort(a, six+1, til);
    }*/
}
